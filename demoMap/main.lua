#include "../scripts/utility.lua"

function tick()

    -- local vehicles = FindVehicles('explosiveCar', true)

    -- for i = 1, #vehicles do
    --     if GetVehicleHealth(vehicles[i]) > 0.2 then

    --         local pos = AabbGetBodyCenterTopPos(GetVehicleBody(vehicles[i]))

    --         DrawDot(pos, 1,1)
    --         PointLight((pos),1,1,1,1)

    --     end
    -- end

end